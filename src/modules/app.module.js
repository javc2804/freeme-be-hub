const debug = require('debug')('BQ360::Log::AppModule');
const bqConfig = require('../config/configuration');

// const mongoose = require('mongoose')
//   Variable = mongoose.model('Variable');

module.exports = () => {
  return {};
};
